# 컨텍스트를 작게: 실행에 필요한 코드만 복사한다
node_modules
dist
.build-cache
.git
*.log
*.out
*.png
*.json.tmp
step2-snapshot.json
flow.log
reserve-fast.log
reserve.log
wait-times.log
runs.log
server.out
server.err

# 개인 값은 이미지에도 담지 않는다 (런타임에 -e KEYESCAPE_NAME/KEYESCAPE_HP 로 주입)
# ※ 접두 ** 없이는 루트 경로만 매칭되므로 ui/ 아래 파일은 그대로 복사됐었다 (실측)
**/local.env
**/.env
**/runs/
**/*.log
**/*.out

# 컨테이너 실행과 무관한 문서·실험 도구·로컬 보관 자료
.local
tools
docs
desktop
MEMORY.md
**/.DS_Store
